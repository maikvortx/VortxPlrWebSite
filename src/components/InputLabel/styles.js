import styled from 'styled-components'
import { paragraph } from '../../assets/ui-kit'

export const StyledInputLabel = styled.label`
  font-weight: 400;
  font-size: 14px;
  margin-bottom: 10px;
  display: block;
  color: ${paragraph.color};
  font-family: ${paragraph.fontFamily};
`
